# Server
PORT=3000

# Where the frontend runs (used for socket CORS)
CLIENT_ORIGIN=http://localhost:5173

# Music API: Audius needs NO API key — just an app name sent on each request.
AUDIUS_APP_NAME=NeroParty
